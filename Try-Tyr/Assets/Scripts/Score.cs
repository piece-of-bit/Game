using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Score : MonoBehaviour
{
    public double MONEY;
    public double price_buy_scale;
    // Start is called before the first frame update
    void Start()
    {
        MONEY = 0;
        price_buy_scale = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
